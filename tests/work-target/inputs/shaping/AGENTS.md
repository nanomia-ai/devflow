# Relay Desk fixture

Before project work, read `.devflow/index.md`. Use only the explicitly selected project-local skill
under `.agents/skills/`; do not discover global, cached, parent-repository, plan, or legacy material.

