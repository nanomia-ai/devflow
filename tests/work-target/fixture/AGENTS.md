# Queue Relay project entry

Read `.devflow/index.md` first. It is the only project map. Documents under `.devflow/` are the
canonical project knowledge; do not depend on files outside this fixture. Use only the explicitly
invoked project-local Devflow target. Work may change only the selected contract's write boundary;
preserve unrelated bytes and report the actual Git safe point and next route.
