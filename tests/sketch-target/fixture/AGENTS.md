# Runbook Signal Sketch fixture

This is a standalone Devflow vNext behavior fixture. Use only the project-local skill explicitly
requested by the user and files inside this fixture as project evidence. Durable project knowledge
lives only under `.devflow/`; preserve unrelated Git bytes.
