# Operations guide

Categories organize expenses and may be renamed. When a category is deleted, its existing expenses
remain in the ledger as `Uncategorized`. CSV backup contains date, amount in cents, category name,
and note; internal identifiers are not portable data.
