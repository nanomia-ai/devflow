# Queue Relay project entry

Read `.devflow/index.md` first. It is the only project map. Documents under `.devflow/` are the
canonical project knowledge; do not depend on files outside this fixture. When the request asks for
direction, use only the project-local `direct` target and do not implement the resulting change.
