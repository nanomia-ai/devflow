# Incident Relay fixture

This is a standalone Devflow vNext behavior fixture. Use only the project-local skill explicitly
requested by the user and the files inside this fixture as project evidence.

There is no external project documentation. Durable project knowledge belongs only under
`.devflow/`; `AGENTS.md` is a host entry, not a second knowledge home. Preserve unrelated Git bytes
and do not inspect the parent Devflow implementation repository as evidence about this project.
