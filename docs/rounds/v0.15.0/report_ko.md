# v0.15.0 구현 보고 — 증거와 계보

작성 2026-08-15 · 최종 계획 `plan3_ko.md`의 §8 순서로 구현한 라운드의 기록.
구현 방식: GPT(gpt-5.6-sol xhigh)와 Claude(fable xhigh)의 교차 검증 루프를 Orca
오케스트레이션으로 운영. 모든 소견은 만든 쪽의 원문 대조 재검토를 거쳐서만 적용됐고,
오케스트레이터가 매 단계 diff를 검수했다.

## 1. 무엇이 실렸나

| 묶음 | 내용 | 커밋 |
|---|---|---|
| 기획 축 (§8.1) | planning-evidence companion 신규(영문 75줄) + product·arch·adopt·split 기획 변경 + DD-67 | `0fb9fe7`→`6d5bb76`→`8f8c73e`→`0fd8c52` |
| 수리 축 (§8.2) | verify의 신호 카드·수리 계보·재관측·유계 route 투영·사람 관문 + verifier label 왕복 + split 상속 + principles 4줄 + DD-68 | `ad7c4c4`→`aefced7`→`c0b06de`→`8934b39` |
| 배선 (§8.2 10단계) | AGENTS 용어표 6행·companion 쌍 등록, repository-invariants fixture 10종, 매트릭스 H42~H45·§3.14~3.15 | `41b545e` |
| 감사 수리 (§8.3 12단계) | 4렌즈 감사 채택 5건(A1~A5) + 판단 3건 착지(관찰 2·개명 1) | `53365d7`→`984dbff`→`c62cc74` |

영문 배포 순증 합계 **273줄** (예산 186~280 안): planning-evidence 75 · product 35 ·
arch 40 · adopt 40 · split 44 · principles 5 · verify+verifier 34. diff 0 계약 파일
(work·reviewer·resume·baseline/verification/state-predicates·README·backlog 항목 외
본문) 전부 바이트 불변 확인.

## 2. 검증 캠페인 — 예측 대 실측

계획의 예측: 텍스트 검증이 실결함을 찾는다(이전 라운드 수렴 13→13→12→13→2).
실측: **교차 검증 루프 3회전 + 4렌즈 독립 감사에서 실결함 10건 채택·수리**, 기각 8건
전건 원문 좌표 증명.

| 단계 | 소견 | 채택 → 수리 | 기각 |
|---|---|---|---|
| 기획 묶음 한국어 (Claude→GPT) | 3 | 2 — companion 차단 정의 극성 반전("있을"→"없을"), adopt 옛 능력 행 형식 | 1 — DD-67 제목 압축 차이(뒤에 소유자 선택으로 통일, `81fc016`) |
| 기획 묶음 영문 (Claude→GPT) | 1 | 1 — split S6 출구의 배타 선택("-거나")이 순차 명령으로 번역된 표류 | 0 |
| 수리 묶음 한국어 (Claude→GPT) | 1 | 1 — 투영 출력 블록의 계획 내부 좌표 "§6.1" 죽은 참조 | 0 |
| 수리 묶음 영문 (Claude→GPT) | 1 | 1 — 라우팅 토큰 단수 인용("fix card")과 정본 복수형 불일치 — 정확 일치 독자의 계보 전면 단절급 | 0 |
| 4렌즈 독립 감사 (신선 4세션) | 15 | 5 — 실패 본문 우측 앵커 판독, split 상속 경로 이상 출구, form2 중단 복구 분기, 사람 관문 뒤 단일 항목 라우팅, 금지 용어 "capability file" 재고정 | 7 — 릴리스 관문 시점 오인 1, 계획이 명시 요구한 문장의 삭제 제안 4, 정본이 이미 답한 독해 2 |

관찰 비승격 21건은 각 보고서에 강등 사유와 함께 남겼다. 잡힌 결함의 공통 모양은
"한 글자·한 단어·한 토큰"이 분기 반전·왕복 부활·계보 단절을 여는 것 — 문자적 독자
상정 검증이 아니면 못 잡는 부류였다.

**소유자 판단 3건의 처분** (상위 관점 재검증: GPT 골격 → Claude 다듬기 → 오케스트레이터
판정, 계보 인용 정정 0):

- C1 교차 능력 수리 계보 route 위치 비대칭 · C2 동일 완료 신호 이중 label — 후속 설계
  라운드를 예약하지 않고 **백로그 관찰 항목 + 정확 시그니처 + 재개 조건**으로 착지.
  근거: DD-44와 v0.11.0 라운드의 직계 선례("교차 능력 의존은 실제 누락 관찰 전 자동
  확장하지 않는다"), 백로그의 관찰 우선 운영 원칙. C2의 동일 신호 묶기는 request 원안에
  있었으나 plan3 P3-D2가 반증으로 대체한 선행 후보라, 재개 시 그 기록된 이유를 먼저
  반박해야 한다.
- C3 등급 개념 세 이름 — `기획 깊이 등급 | planning depth grade`로 양 언어 단일화,
  용어표 등록.

**추가 타당성 검사(§9.4 5질문)**: 우월 대안 0. §14.2의 열 가지 설계 비교 중 아홉이
배포 문장에서 그대로 유지, 하나(L21 지연 명시)는 감사 소견과 동일 지점의 문구 이행
사안으로 판명되어 A4로 수리됨. 새 영구 필드 셋 각각의 제거 대안, 투영 시점·범위 대안,
의미 fingerprint, planning-frontier 영속이 전부 L 좌표 대입으로 반증됐다.

**수정분 유계 재감사**(신선 세션): 채택 소견 0건 · 관찰 4건 · L6/L9/L10/L14/L18/L20/L21
재걷기 전부 통과 · 수리 문장 ko-en 의미 동형 확인 · 기계 검사 95/95. §9.5 종료 조건
전부 충족으로 텍스트 검증 캠페인 종료.

## 3. 계획 블록 이탈 — 1건 (기록 의무)

- plan3 §6.3 출력 형식 넷째 줄의 계획 내부 자기 참조 `§6.1`을 배포 본문의 같은 절에서
  정의한 `직전 수리 라운드`로 재고정해 한 토큰 삭제했다. 필드명·자료형·정렬·선택 의미는
  바꾸지 않았다. 그 외 계획 명세 이탈 0 — 구현 재량 안의 판단(§14.1류 배치 교정 2건,
  form2 복구 분기 명문화 등)은 전부 계획의 의도를 보존하는 방향이었고 각 보고서에 기록됐다.

## 4. 기계 검사와 측정

- `node --test`: 95개 중 94 통과가 캠페인 내내 유지(유일 실패는 이 보고서 자신의 부재를
  잡는 경로 검사 — 이 파일 착지로 해소). 새 fixture: 실패 이력 3형식 우측 앵커 파싱,
  route 투영 10종(새 root·재관측 1/2+·혼합·모호 root 차단·C1+C2+C3 상속·비카드 건너뜀·
  정본 정렬), planning-evidence 소비자 정확 4, 라우팅 토큰 복수형.
- ko↔en 구조 동형·배포물 한국어 0(README 1)·결정 색인/본문 ID·manifest 동형: 전부 통과.
- 실측 유보: plan3 §10의 런타임 측정 항목(사실 질문 횟수·격리 조사 발동 수·root 복원률·
  상속 카드 수·정상 pass 투영 비용 등)은 **실전 사이클 0회**라 미측정이다. 이 보고서는
  계획의 점수를 성과로 쓰지 않는다 — 측정은 다음 실전 프로젝트(nanomia-ade)의 몫이고,
  관찰 시그니처 2건이 백로그에 대기한다.

## 5. 정본 승격 기록

- DD-67(기획 증거 규율)·DD-68(수리 계보) — 색인·본문 착지, 색인 행과 절 제목 동일
  (소유자 선택).
- 용어표 6행: 기획 증거 규율 · 수리 계보 · 재관측 · 구속 전 재검토 · 신호 카드 ·
  기획 깊이 등급. companion 쌍 1행. "companion canon" 표기는 canonical companion으로
  전역 단일화(잔존 0).
- 매트릭스: H42~H45 신설 + §3.14~§3.15 교차 워크 + 기존 영향 칸 재판정.
- 백로그: 관찰 항목 2건(교차 능력 route 비대칭 · 동일 신호 이중 label — 각각 시그니처와
  재개 조건 포함, 양 언어).
- 차용 경계: decision frontier·사실/결정 분리·경계 시나리오·1차 출처 규율·답만 필요한
  근거 탐색의 격리 조사 차용, 무제한 grilling·조사 파일층·간접 skill 의존·3-agent 설계
  비차용 — design.md에 착지.

## 6. 운영 기록 (이 라운드의 방법)

- 소유자 결정 이력: 구현 승인 → 기획 묶음 한국어 검토를 루프 검증으로 갈음 → GPT 전권
  실행·세션 유지 지시 → 수리 묶음 자동 진행 위임 → 감사는 명백 자동 수리+판단만 보고 →
  DD-67 제목 통일 → C1~C3 상위 재검증 요청과 그 결과 수용 → GitHub push는 종결 시.
- 소견 처리 규율: 신선 감사자 주장 → 구현 맥락 세션의 원문 반증 → 오케스트레이터 최종
  판정의 3중 거름. 기각에도 원문 좌표 증명을 의무화했고, 구조 간과형 오견 7건이 이
  거름에서 걸러졌다.
- 커밋 계보 15건이 과정 전체를 추적 가능하게 남긴다(`c37a0a8`…`c62cc74`).
