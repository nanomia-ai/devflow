# devflow 설계 문서 — 왜 이렇게 만들었나

이 문서는 devflow의 **"왜"의 정본**이며, 모든 변경에서 항상 읽히는 한 층이다.
정체성과 철학, 건드리면 안 되는 불변식, 그리고 결정 전부의 색인이 여기 있다.
결정의 전문과 기각 계보는 `design-decisions_ko.md`, 관찰 항목과 보류 후보는
`design-backlog_ko.md`에 있고, 다른 문서들이 각각 무엇을 소유하고 언제 읽히는지는
아래 「문서 지도」가 정한다.

**여기 기록된 결정을 뒤집으려면, 기록된 이유부터 반박해야 한다.**
기각된 안을 재제안하려면, 기록된 기각 사유부터 반박해야 한다.
이유 반박이 없는 번복·재제안은 검토를 통과하지 못한다.

## 기원과 철학

사용자(jmp)가 AI와의 개발 전 과정(기획→구현→검증)을 관리하기 위해 설계했다.
2026-08-05, 사용자와 v0 → v6 여섯 차례 왕복하며 개념을 확정한 뒤 구현했다.

핵심 철학 — 모든 수정은 이 기조를 지켜야 한다:

1. **풍부한 방향성 + 최소 하네스.** 최신 상위 모델은 방법을 안다. 목적지와 금지만 명확히
   주고 방법은 지시하지 않는다. 하네스는 모델 등급에 반비례해서만 강화한다.
2. **지식은 적지 않는다, 취향은 적는다.** 모델이 이미 아는 보편 원칙(인젝션 방어법 등)을
   적는 것은 세금이다. 이 프로젝트가 무엇을 우선하는지(선언)만 적는다.
3. **진행 상태는 문서가 아니라 파일 트리다.** 파일명 접미사(.wip./.done./.stale.)와
   위치가 정본. 문서에 진행률을 적으면 반드시 낡는다. 작업 진행은 접미사와 진행 로그가
   소유하고, 규칙 정본이 형식을 고정한 journal·verify.md 상태 줄은 전이 상태일 뿐 진행
   기록이 아니며, 문서에는 여전히 진행을 적지 않는다.
4. **1개념 1단어.** 스킬 이름 = 산출물 이름 = 그 개념의 유일한 단어.

## 구조 한눈에

```
Layer 0 (1회·상속): product → arch → [design] · 기존 코드는 adopt가 역산     Layer 1 (반복): split → work ⇄ verify
공통: resume, principles(규칙 정본)
대상 프로젝트에 생기는 것: devflow/{project/, tree/, journal.md, users/<id>/ 방}
배포: Claude 플러그인(.claude-plugin) + Codex 네이티브 플러그인(.codex-plugin — codex/install.*가 등록)
```

## 문서 지도 — 무엇이 어디에 살고 언제 읽히나

| 문서 | 지위 | 언제 읽히나 |
|---|---|---|
| `docs/design_ko.md` (이 문서) | 정본 — 정체성 · 불변식 · 결정 색인 | 모든 변경에서 항상 |
| `docs/design-decisions_ko.md` | 정본 — 결정 전문과 기각 계보 | 색인의 한 행이라도 움직일 때 |
| `docs/design-backlog_ko.md` | 정본 — 관찰 항목과 보류 후보 | 릴리스를 기획할 때 |
| `AGENTS.md` | 절차 — 게이트 · 번역 · 릴리스 · 라운드 규약 · 승격 | 세션 시작에 자동으로 |
| `docs/audit-guideline_ko.md` | 상시 수단 — 검증 방법의 정본 | 검증 결과를 보고하려 할 때 |
| `docs/usecase-matrix_ko.md` | 상시 수단 — 사용 형태 전수 | `skills/**`를 바꿀 때 |
| `docs/rounds/<버전>/` | 라운드 기록 — request · handoff · plan · report · audit | 새 라운드를 열 때 직전 것만 |
| `README.md` | 사람용 — 무엇을 어떻게 쓰는가 | 사용자가 읽는다 |
| `CHANGELOG.md` | 이력 — 버전별로 출시된 것, 0.10.0 이후. 배포 변경만 | 출시된 동작이 언제 바뀌었는지 찾을 때 |
| `docs/changelog-archive.md` | 이력 — 0.10.0 이전의 출시 변경 | 0.10.0보다 오래된 것을 찾을 때 |
| `skills/principles/SKILL.md` | 규칙 정본 — 런타임이 실행하는 것 | devflow가 도는 모든 세션 |

두 상시 수단은 이 저장소를 고치는 세션이 **스스로** 여는 것이지 소유자가 브리핑하는 것이
아니다. 무엇이 그것을 열게 하는지는 `AGENTS.md`의 배선표가 정한다.

라운드 기록은 2026-08-13에 평면 `docs/`에서 `docs/rounds/<버전>/`으로 내려갔고 파일명이
역할(handoff · plan · report · audit)로 통일됐다. 그 이전 경로를 가리키는 `CHANGELOG.md`의
문장은 그때의 사실이므로 고치지 않았다 — 옛 경로는 그 버전의 라운드 폴더에서 찾는다.

## 불변식 — 반박 전에는 건드리지 않는 것

아래 항목들은 비용이 곧 기능이라 최적화 대상이 아니다. 원전은 0.13.0 기획 §14이고, 아래는
v0.14.0 실행 리포트 §6이 접어 둔 요지를 그대로 옮긴 것이다. 이 목록은 승격 표를 통해 자라므로
개수를 본문에 적지 않는다.

- **카드마다 Layer 0 재읽기** — 압축 뒤에는 "읽었다"가 "가지고 있다"가 아니다.
- **검토는 클린 세션으로** — 구현 이력이 판단을 물들인다.
- **폐쇄 시 검증 구역 통째 교체** — 부분 갱신은 모순을 남긴다.
- **진행 로그는 실행 전에 갱신** — 죽는 순간을 모르므로 "나중"이 곧 소실이다.
- **점유=커밋** — 동시 작업 전체가 이 위에 선다.
- **resume의 정합성 상시 실행** — 병합 사고를 잡는 유일한 그물이다.
- **`Read first` 전부 열기** — 탐색을 대체하는 장치 자체다.
- **폐쇄 때의 verify · 감리 · 회고** — 지식이 능력 문서에 쌓이는 그 순간이다.

devflow가 **막아 주지 않는다고 선언한** 자리는 별개의 목록이며 `README_ko.md`의
「보장하지 않는 것」(영문 `README.md`의 "What this does not cover") 표가 정본이다. 그
표의 항목을 없애자는 제안은 여기 결정을 뒤집는 것과 같은 관문을 통과해야 한다.

## 결정 색인 — 전부 읽고, 이번 변경이 어느 것도 바꾸지 않는지 진술하라

이 색인은 항상 읽는다. 한 행이라도 바꾼다면 그 주제 절을 `design-decisions_ko.md`에서 연다.
기각은 여기 색인하지 않는다 — 제안하려는 것의 주제 절을 열면 그 주제의 결정 뒤에 그 주제의
기각 계보가 함께 있다. 기각된 안을 재제안하려면 거기부터 읽는다.

| ID | 결정 | 주제 | 도입 | 상태 |
|---|---|---|---|---|
| DD-01 | 산출 폴더명 `devflow/` (docs/ 아님) | 정체성 · 배포 · 플랫폼 | 최초 설계 | 유효 |
| DD-02 | 이름은 devflow — Claude는 `devflow:` 네임스페이스, Codex는 `devflow-` 파일명 접두 | 정체성 · 배포 · 플랫폼 | 최초 설계 | 유효 |
| DD-03 | 규칙 정본이 skills/principles/ 안에 있음 | 정체성 · 배포 · 플랫폼 | 최초 설계 | 유효 |
| DD-04 | Codex 프롬프트에 정본 동봉(파일 참조 아님) | 정체성 · 배포 · 플랫폼 | 최초 설계 | 대체됨 → DD-57 (v0.13.0) |
| DD-05 | 훅은 SessionStart 하나만 | 정체성 · 배포 · 플랫폼 | 최초 설계 | 유효 |
| DD-12 | 스킬 상호참조는 슬래시 없는 단계명 | 정체성 · 배포 · 플랫폼 | 최초 설계 | 유효 |
| DD-13 | install.ps1은 UTF-8 **BOM** 필수 | 정체성 · 배포 · 플랫폼 | 최초 설계 | 유효 |
| DD-16 | 설계는 한글, 배포는 영문 (이중 언어) | 정체성 · 배포 · 플랫폼 | 최초 설계 | 유효 |
| DD-18 | Codex 설치는 네이티브 플러그인 채널이 1차(마켓플레이스 등록 + plugin add), 생성 슬래시 프롬프트는 명시 호출 채널로 유지, 훅은 ~/.codex/hooks.json 별도 등록 | 정체성 · 배포 · 플랫폼 | v0.9.9 | 유효 · 일부 정정 → DD-57 (v0.13.0) |
| DD-29 | 플랫폼 어댑터는 공통 스킬로 연결만 하고 절차를 복제하지 않는다 | 정체성 · 배포 · 플랫폼 | v0.9.21 | 유효 |
| DD-32 | Codex 훅은 플러그인이 배달한다 — `.codex-plugin/plugin.json`이 `hooks`를 선언, 설치는 원격 두 줄 | 정체성 · 배포 · 플랫폼 | v0.9.20 | 유효 |
| DD-57 | Codex 평면 프롬프트 채널을 없애고 플러그인 캐시가 동반 문서를 나른다 | 정체성 · 배포 · 플랫폼 | v0.13.0 | 유효 |
| DD-08 | TDD 절차 미채택 | 검증과 역할 | 최초 설계 | 유효 |
| DD-17 | 검토·검증 역할의 조건은 스킬 본문이 싣는다 — agents/*.md는 Claude 포장 | 검증과 역할 | v0.9.5 | 유효 |
| DD-19 | 역할 계약은 스킬 옆 동반 파일 하나(reviewer.md·verifier.md)이고, 모든 플랫폼이 깨끗한 컨텍스트에 원문 브리핑으로 수행한다 — Claude 에이전트 등록 없음 | 검증과 역할 | v0.9.6 | 유효 |
| DD-21 | 감리(audit) — 사건 발동 깊은 점검, 소견은 판정이 아니다 | 검증과 역할 | v0.9.15 | 유효 |
| DD-22 | 회고(retrospective) — MVP 경계에서 설계 대안을 사후 평가하는 4번째 역할, 소견은 판정이 아니다 | 검증과 역할 | v0.9.16 | 유효 |
| DD-23 | 회고는 능력이 처음 닫힐 때도 돈다 — 그 능력 범위로 | 검증과 역할 | v0.9.17 | 유효 |
| DD-24 | 신호 통과는 입력이 바뀌면 낡는다 + 수정 카드의 완료 신호는 verifier의 재현 절차 | 검증과 역할 | v0.9.4 | 유효 |
| DD-30 | 검증 실패·수정 가능한 미검증·감리·회고 사건은 verify.md 덮어쓰기와 세션 중단 뒤에도 남는다 | 검증과 역할 | v0.9.21 | 유효 |
| DD-36 | 능력 통과 게이트는 판정과 폐쇄 사이의 명시 상태이고, 폐쇄는 검증 revision 입력을 바꾸지 않는다 | 검증과 역할 | v0.9.21 | 유효 |
| DD-41 | Record는 자기 항목 수를 기술한다 — New entries | 검증과 역할 | v0.9.23 | 유효 |
| DD-06 | 모델명을 파일에 안 적음 (T-상/중/하 등급만) | 작업 트리와 카드 | 최초 설계 | 유효 |
| DD-09 | 1 작업 = 1 커밋 (검증 통과 후) | 작업 트리와 카드 | 최초 설계 | 유효 |
| DD-11 | 정합성 점검은 보고만, 자동 교정 금지 | 작업 트리와 카드 | 최초 설계 | 유효 |
| DD-25 | 준비된 카드의 정의·카드별 실행 제안 승인·재개 라우팅은 디스크 상태로 판정한다 | 작업 트리와 카드 | v0.9.21 | 유효 |
| DD-27 | `.stale.` 작업 카드는 역사로 남고 `재분할 대기` 마커가 교체 계획 전 중단을 복구한다 | 작업 트리와 카드 | v0.9.21 | 유효 |
| DD-38 | `승인`의 유효성은 카드 값만이 아니라 Git diff 신선도까지 요구한다 | 작업 트리와 카드 | v0.9.21 | 유효 |
| DD-50 | 변경 요청은 즉시 기록하고 나중에 계획한다. 완료 신호는 그 능력으로 좁힌다 | 작업 트리와 카드 | v0.12.0 | 유효 |
| DD-54 | 여러 능력에 걸친 요청은 출처 하나와 부모마다 마커 하나를 갖는다 | 작업 트리와 카드 | v0.13.0 | 유효 |
| DD-55 | 전제→결과 전이를 바꾸지 않는 항목들은 카드 하나에 함께 탄다 | 작업 트리와 카드 | v0.13.0 | 유효 · 일부 정정 → DD-61 (v0.14.0) |
| DD-58 | 끝난 카드의 번호는 다시 매기지 않는다 | 작업 트리와 카드 | v0.13.0 | 유효 |
| DD-61 | 경량 변경(tweak)은 커밋이 곧 기록이다 | 작업 트리와 카드 | v0.14.0 | 유효 · 일부 정정 → DD-66 (v0.14.2) |
| DD-65 | 혼합 요청은 게이트 탈락 항목만 기록한다 — 통과 항목은 journal 줄에 적히지 않는다 | 작업 트리와 카드 | v0.14.2 | 유효 |
| DD-07 | Git worktree 운용을 devflow가 만들거나 관리하지 않음 | 동시성 · 점유 · 통합 | 최초 설계 | 유효 |
| DD-14 | 다중 모드의 분할 축은 사람이 아니라 **진실의 범위** | 동시성 · 점유 · 통합 | 최초 설계 | 유효 |
| DD-15 | HANDOFF는 git에 커밋하되 전용 커밋 금지 — 경계 정리 커밋에 동승만 | 동시성 · 점유 · 통합 | 최초 설계 | 유효 |
| DD-31 | 다중의 공유 라우팅 상태는 로컬 브랜치가 아니라 통합 브랜치 tip에서 읽는다 | 동시성 · 점유 · 통합 | v0.9.21 | 유효 |
| DD-40 | 승인된 병렬 묶음은 한 단계에서 함께 점유된다 | 동시성 · 점유 · 통합 | v0.9.23 | 대체됨 → DD-60 (v0.14.0) |
| DD-46 | 모드는 하나 — 방은 항상 있고, 혼자 쓰면 통합 브랜치가 지금 쓰는 브랜치로 접힌다 | 동시성 · 점유 · 통합 | v0.12.0 | 유효 |
| DD-47 | 점유는 깊이 1 단위 축, 선택은 정본 후보 순서 하나 | 동시성 · 점유 · 통합 | v0.12.0 | 유효 · 일부 정정 → DD-60 (v0.14.0) |
| DD-49 | Git은 요건이고, 워크트리가 흐름 등록부다 | 동시성 · 점유 · 통합 | v0.12.0 | 유효 · 일부 정정 → DD-51 (v0.13.0) |
| DD-51 | 공유 진실은 통합 브랜치 하나이고, 다른 워크트리의 HEAD는 권위가 아니라 증거다 | 동시성 · 점유 · 통합 | v0.13.0 | 유효 |
| DD-52 | 공유 전이는 기억해 둔 통합 id에 대고 게시한다 | 동시성 · 점유 · 통합 | v0.13.0 | 유효 · 일부 정정 → DD-62 (v0.14.0) |
| DD-53 | 한 작업 폴더의 여러 세션은 정상이고, 그 안전은 락이 아니라 측정된 다섯 줄이다 | 동시성 · 점유 · 통합 | v0.13.0 | 유효 |
| DD-60 | 점유는 자유 병렬이고, 체크포인트는 이 세션이 만든 변경만 담는다 | 동시성 · 점유 · 통합 | v0.14.0 | 유효 |
| DD-62 | 봉쇄 중에도 발급·점유·소비가 아닌 journal 덧붙임은 즉시 기록한다 | 동시성 · 점유 · 통합 | v0.14.0 | 유효 |
| DD-63 | journal 병합 충돌은 합집합이 아니라 3-way다 | 동시성 · 점유 · 통합 | v0.14.0 | 유효 |
| DD-66 | 경량 차선은 커밋의 착지를 기계로 먼저 확인하고, 같은 파일 경합에서는 경량이 양보한다 | 동시성 · 점유 · 통합 | v0.14.2 | 유효 |
| DD-28 | 영속 지식은 새 문서층이 아니라 범위가 고정된 기존 기록의 소비 경로로 잇는다 | 지식층과 능력 문서 | v0.9.21 | 유효 |
| DD-33 | 지식 도달성 묶음 — 밖 기록 지위 선언 · 대화 결정 즉시 착지 · product 핵심 4절 확인 게이트 · 반증 행(서술 교체 또는 product 재실행) · 확인 수단 행 · HANDOFF 전 착지 확인 · 조사 답이 도구일 때의 생존 경로 · verify 실패의 반증 중재 | 지식층과 능력 문서 | v0.9.18 | 유효 |
| DD-42 | 능력 지식 기준선 — 검증 폐쇄가 낳는 도메인 설계도 | 지식층과 능력 문서 | v0.10.0 | 유효 |
| DD-43 | 능력 문서는 Layer 0에서 태어나는 설계 구역과 폐쇄에서 갱신되는 검증 구역을 물리적으로 나누며 항상 켠다 | 지식층과 능력 문서 | v0.11.0 | 유효 |
| DD-44 | 도메인 도달은 카드 필드가 아니라 깊이 1 번호 규칙과 resume의 도메인 진입 분기가 소유한다 | 지식층과 능력 문서 | v0.11.0 | 유효 |
| DD-45 | 능력 문서의 복구는 HEAD를 기준으로 판정하고, 중단된 설계 쓰기는 바이트 대조가 아니라 재생성으로 끝낸다 | 지식층과 능력 문서 | v0.11.1 | 유효 |
| DD-48 | HANDOFF에서 죽던 지식이 두 개의 키 달린 줄로 착지한다 | 지식층과 능력 문서 | v0.12.0 | 유효 |
| DD-56 | 읽기는 열린 작업에만 비례한다 - `.done` 깊이 1 폴더는 이름으로 읽는다 | 지식층과 능력 문서 | v0.13.0 | 유효 |
| DD-59 | 열린 결정은 journal에 살고, HANDOFF에는 트리가 다시 계산하는 것만 남는다 | 지식층과 능력 문서 | v0.13.0 | 유효 |
| DD-64 | 공용 계약 관측의 셋째 갈래는 기명 미결 항목이다 | 지식층과 능력 문서 | v0.14.0 | 유효 |
| DD-10 | 트리 소급 기록 금지 (브라운필드) | 브라운필드와 진입 | 최초 설계 | 유효 |
| DD-20 | 브라운필드 진입은 전용 스킬 adopt — arch에서 분리 | 브라운필드와 진입 | v0.9.10 | 유효 |
| DD-26 | 브라운필드와 층 전환은 별도 디스크 상태를 가져 중단 뒤에도 원래 뜻을 보존한다 | 브라운필드와 진입 | v0.9.21 | 유효 |
| DD-34 | Git 작업 트리의 열린 rebase·merge는 모든 devflow 라우팅보다 먼저 사용자에게 돌린다 | Git 기계와 중단 복구 | v0.9.21 | 유효 |
| DD-35 | Commit locator는 Git의 전체 object ID를 쓰고 경로·카드 순서는 byte·숫자 규칙으로 정본화한다 | Git 기계와 중단 복구 | v0.9.21 | 유효 |
| DD-37 | 원격 증거는 `증거 대기`·`증거 마감` 두 상태로 최종 작업 커밋을 나눈다 | Git 기계와 중단 복구 | v0.9.21 | 유효 |
| DD-39 | tree 입력 revision의 hash는 Windows에서 `cmd /d /s /c` 안의 이진 파이프로만 계산한다 | Git 기계와 중단 복구 | v0.9.21 | 유효 |

## 차용과 그 경계

맷 포콕(mattpocock) skills 저장소에서 차용한 것: 조사 카드(prototype+wayfinder의 증류),
ADR 3조건(domain-modeling), 검증 이중축(code-review), code-style 지향 절의 가치 선언 일부
(codebase-design·tdd에서 절차를 빼고 취향만).

**차용하지 않기로 한 것**: 어휘 강제, Red-Green 절차, 냄새 12종 목록, 3에이전트 병렬 설계.

사용자 규칙: 이 저장소에서 추가 차용 시 반드시 사전 허락.

## 이 문서군을 고치는 법

- **새 결정은 다음 번호를 받는다.** `DD-` 번호는 재사용하지 않는다 — 라운드 기록이 그
  번호로 결정을 인용하기 때문이다. 기각은 `DR-`로 같은 규율을 따른다.
- **뒤집힌 결정은 지우지 않는다.** 본문을 그대로 두고 상태만 바꾼다. 상태는 셋뿐이고 형식이
  고정돼 있다(테스트가 강제한다): `유효` · `대체됨 → DD-nn (vX.Y.Z)` · 결정의 일부만
  물러났다면 `유효 · 일부 정정 → DD-nn (vX.Y.Z)`. 무엇이 그것을 대체했는지가 다음 재제안이
  반박해야 할 대상이다.
- **색인과 본문의 ID 집합은 같아야 한다.** 어긋나면 테스트가 붉어진다.
- **라운드 기록에서 무엇이 여기로 올라오는지**는 `AGENTS.md`의 「승격」 표가 정한다.
  이 문서군은 그 표가 지정한 것만 받는다.
