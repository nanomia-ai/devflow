# DD-80 · 기계 판정의 실행자는 코드이고, 그 판정을 적은 문서는 마지막 런타임 독자가 옮긴 다음 릴리스에서 삭제된다

- 상태: 유효 · 일부 정정 → DD-83 (v0.19.0)
- 주제: 정체성 · 배포 · 플랫폼
- 도입: v0.18.7
- 날짜: 2026-08-20
- 날짜: 2026-08-20

관측된 문제: `resume` 진입이 대상 프로젝트 파일을 한 글자 읽기 전에 정본 넷 **137,514 B**를
낸다(0.18.6 실측 — resume 30,477 + principles 67,498 + 상태 판정 정본 2,114 + 검증 판정 정본
3,495 + 기준선 판정 정본의 캡슐 게이트 밖 33,930). 그 정본의 대부분은 모델의 판단을 요구하는
규칙이 아니라 **디스크와 Git 사실만으로 참·거짓이 갈리는 계산**이다 — 정본 295행을 전수
분류하니 도구가 실행할 수 있는 것이 221행, 사람에게 하는 말과 의미 판단이 23행, 대화·승인이
걸리는 경계가 51행이었다. resume의 라우팅 표 48행 중 **대화 내용을 읽는 행은 한 줄**뿐이다.
그리고 그 계산은 산문이 문자로 적어 두었기 때문에 모델이 손으로 실행한다 — 「NUL로 나눠라,
`cmd /d /s /c` 안에서 돌려라, `--no-renames`를 빼지 마라」 같은 문장이 그것이다.

원하는 행동: 그 계산을 읽기 전용 도구가 실행한다. `resume`은 도구를 **한 번** 부르고, 도구가
낸 상태 한 장에서 첫 번째로 비어 있지 않은 구역의 **행동측만** 산문에서 읽고, 보고하고,
승인을 받고, 스테이지를 부른다. 정본세는 없어지는 것이 아니라 **승인 뒤 실제로 진입하는
스테이지가 낸다.**

고른 경계 넷:

① 도구는 **정본과 같은 자리**(`skills/principles/scripts/project-state.mjs`)에 산다 — 정본을
부르는 것과 같은 상대 경로 규칙으로 닿고, 세 설치 채널 모두에서 정본과 함께 여행한다.
v0.18.6이 여덟 자리에서 없앤 `<플러그인 루트>` 해석 결함을 새 자리에 다시 만들지 않는다.

② 도구가 소유한 판정의 내부를 산문이 **재서술하지 않는다**(`baseline-predicates.md:348-349`가
캡슐 도구에 이미 쓴 문장).

③ **마지막 런타임 독자가 도구로 옮겨 간 릴리스의 다음 릴리스에서 그 술어 문서를 삭제한다.
과거는 git이 진다.** 사람에게 하는 문장·의미 판단·승인 규율은 산문의 소유다.

④ **도구는 사실을 내고 대화를 받지 않는다.** 부름은 `state` 하나이고 입력은 `--root`와
필터 하나(`--capability`)뿐이다. 출력은 우선순위 순 **구역 열넷**이고 **빈 구역도 줄을
찍으며**, 맨 끝의 `next:`는 첫 번째로 비어 있지 않은 구역과 그 종류다 — 사실에서 파생한
요약이지 계약이 아니다. 세션이 아는 것(지목된 카드·미룬 것·이 대화의 요청)은 도구에
부호화해 넘기지 않고 모델이 쓴다.

v0.19.0의 부분 정정(DD-83): 읽기 전용·무상태는 그대로이고, ④의 입력과 출력 계약만 좁혀졌다.
소비자가 하나도 없던 `--card`는 제거한다 — 파싱만 되고 아무 판정도 바꾸지 않았다.
`--capability`는 **판단 전 snapshot 필터가 아니라 판단 뒤 render 필터**다: 전역 정합성은 항상
전체 snapshot으로 계산하고, claim·ready·blocked·layer·carry처럼 능력별인 줄만 렌더에서
좁힌다. 단, 값비싼 능력 문서 detail은 전역 소비자가 없으므로 번호를 받았을 때 그 능력 하나만
계산한다. git·integrity·transition·marker·setup·request·event·product·complete 사실은 좁히지
않으며, 실제로 줄 것이 없으면 거짓 재호출 권고 대신 `narrow=none`을 보인다. 그리고 경계
완전성의 `missing`·`carry`와 `digest`·`origin`은 새 판정이 아니라 **같은 디스크·Git 사실
방출의 확장**이다.

왜 그 경계가 필요한가: 재서술을 허용하면 문서와 코드가 갈리고, 갈린 순간 어느 쪽이 정본인지
알 수 없다. 「사양서로 남긴다」는 두 번째 거처이고, 두 거처가 생기는 순간 그 둘을 묶을 기제가
존재할 이유가 생긴다 — **거처를 하나로 만들면 기제가 필요 없다.** 그리고 도구를 정본 밖에
두면 「정본은 따라갔는데 실행자는 안 따라갔다」는 장면이 생겨 그것을 메울 파일이 또 필요해진다.
④가 필요한 이유는 따로다: 세션 사실을 플래그로 부호화하려면 모델이 그 사실을 **먼저 기억하고
있어야** 하고, 기억이 이미 모델에 있으면 그것으로 직접 판단하는 것과 부호화해 도구에 넘기는
것의 차이는 왕복 한 번과 계약 표면 여덟 개뿐이다. 플래그는 기억을 대신하지 않고 옮겨 적는다.

기각한 대안: **결합 키 53개·서브커맨드 여섯·세션 플래그 여덟** — 계약 표면이 커지는 만큼
산문이 규약을 지고, 키가 틀렸는지 알려면 조건측을 다시 열어야 해 전수 판정을 내는 별도
플래그가 필요해진다. 사실 구역 열넷과 빈 구역 인쇄가 같은 정보를 **더 적은 계약**으로 낸다.
**문서를 코드에서 생성** — 산문이 나르는 *이유*를 코드가 못 담는다. **문서를 「사양서」로 영구
보존** — 하나의 법칙에 어긋나고 DD-25의 "한 번 소유한다"가 글자 그대로 참이 되지 못한다.
**도구를 `scripts/`에 두고 조건측 산문을 `skills/`에 남김** — 파일 하나·ko/en 짝 하나·기제
하나가 늘고, v0.18.6이 없앤 자리표시자 해석 결함이 새 자리에 다시 생긴다. **도구 없이 산문만
축약** — 앞선 세대가 이미 실패했고 회수 총량이 5–10 KB에 그친다.

영향 좌표: `skills/principles/scripts/project-state.mjs`(신규 88,382 B),
`scripts/project-state.test.js`(신규 37,898 B · 신규 시험 114),
`skills/resume/SKILL{,_ko}.md`(재작성 — 30,477→21,713 B · 32,042→22,893 B),
`skills/{verify,work,split,arch,adopt}/SKILL{,_ko}.md`(진입 문장과 그 판정을 쓰는 자리),
`skills/principles/{state,verification}-predicates{,_ko}.md`(어느 스킬도 읽지 않는다 — 다음
릴리스에서 삭제), `skills/principles/baseline-predicates{,_ko}.md`(resume 소비자에서 제외),
`scripts/repository-invariants.test.js`, `docs/design{,_ko}.md`의 스킬 의도 색인 두 행,
`AGENTS.md` 배선 표 한 행, `docs/maintenance-protocol{,_ko}.md` §9 용어 둘,
`docs/design-backlog{,_ko}.md` 관측 한 줄. v0.19.0의 정정분은 DD-83의 좌표가 소유한다.

다시 볼 조건: 제3자 skills.sh 설치가 실제로 쓰이고 그 채널이 `skills/<name>/scripts/*.mjs`를
못 나르는 것이 관측될 때 · 라우팅 표의 대화 의존 행이 한 줄을 넘어 늘 때 · 무결성 14항의
프리픽스 절을 v1이 `prefix=unchecked`로 남긴 것이 실전에서 결함을 놓쳤을 때 · 실전에서
모델이 `next:`와 다른 구역으로 간 세션이 관측되고 그 원인이 「사실이 보였는데 순서를 잘못
읽음」일 때(그때 움직이는 것은 키 레지스트리의 부활이 아니라 `next:` 한 줄의 지위다)

## 정본 토큰

이 결정이 정의하거나 참조하는 정본 문자열이다. **번역하지 않는다** — 문장은 한국어라도
이 토큰은 grep 좌표이므로 원문 그대로 유지한다.

- `<plugin root>`
