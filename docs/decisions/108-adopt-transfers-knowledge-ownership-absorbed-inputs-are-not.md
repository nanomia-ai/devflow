# DD-108 · Adopt는 지식 소유권을 이전하며 흡수 입력은 영구 프로젝트 의존성이 아니다

- 상태: 유효
- 주제: 지식층과 능력 문서
- 도입: v0.23.16

관측된 문제: Adopt는 역산 중 모든 유지 자료를 올바르게 회계했지만, 그 이관 inventory를
Architecture `Existing records`와 K footer·본문 provenance mark에 영속시켰다.
성공한 도입 뒤 입력 문서를 삭제하거나 크게 고치면 현재 지식이 검증에 실패하거나 후속 독자가
옛 자료를 다시 열어야 했다. 목적·도메인 의미·결정 이유·제약·용어·modality·불확실성을 옮기는
일시 증거였던 문서 경로가 두 번째 소유자가 된 것이다.

선택한 경계: 원문 좌표·권위·처분·착지 target은 완전하지만 일시적인 제안·반증 증거다. 승인된
최종 owner/K는 자기완결 현재 지식이고, Architecture `Existing records`에는 소유자가 계속
유지하겠다고 명시한 외부 입력만 남는다. 흡수 이관 입력 뒤의 K에는 보통 `Source basis`가 없다.
사용자가 유지한 live input 또는 기존 managed card가 footer를 공급할 때는 배열이 비어 있지 않고
모든 좌표가 이전과 같은 경로·revision·줄 범위 검사를 통과해야 한다. 부재와 강하게 유효함만 있다.
본문 좌표도 현재 code·소유자 유지 live input·managed-card evidence만 가리킨다. 흡수 입력의 의미·
modality·기각 방향·불확실성은 자기완결 owner 산문에 살고, 미해결 모순은 두 입장과 근거를 machine
coordinate mark 없이 보존한다.

DD-97의 두 커밋은 유지하되 staging partition은 self-reference 경계를 따른다. Adopt는 capability
`Design head: none`으로 승인된 Layer 0·capability·K 전체를 쓰고 검증한 뒤 모든 owner·same-owner K·
정확한 follow-on을 첫 커밋에 착지시킨다. Design-head command의 pathspec은 product·architecture·
glossary만 이름 대므로 capability-only 둘째 커밋은 그 명령이 선택한 첫 커밋을 전진시키지 않는다.
둘째 커밋은 capability Design head 줄만 그 첫 커밋 ID로 바꾼다. 첫 커밋 뒤 승인된 의미는 모두 정본이고 committed `none`은 Resume→Arch가 회복하는
stale baseline이며, uncommitted bytes는 복구 입력이 아니다. 새 state·marker·predicate·ledger·
commit-body mapping은 없다.

이유와 기각안: 역사 좌표를 호환 표지와 함께 남겨도 dangling 의존성은 남는다. 좌표 검증을
약화하면 live input과 managed card가 거짓말할 수 있고, 모든 좌표를 금지하면 기존 card 근거가
깨진다. 단일 커밋은 Design head가 요구하는 실제 Layer 0 hash를 없앤다. 영속 이관 ledger나 새
복구 상태는 소유권을 중복시키고 폐기한 광역 재설계를 되풀이한다. 따라서 transient 회계가
self-contained 지식이 되고, 근거가 있을 때만 optional footer가 강하게 유효한 하나의 인과로 고친다.

영향 좌표: Adopt 행동·authoring card·제안·출력 template, 공통 capsule·freshness·writer·commit·input
정책, Product·Design·Arch 공통 K template, 두 Architecture template와 Arch schema 한 문장, 문서
변경 routing의 Adopt 한 문장, root·deployed knowledge validator pair와 표적 시험, DD-76·DD-97,
matrix §3.18·§3.24, 설계 지도, 생성 영수증, plugin manifest, CHANGELOG, v0.23.16 보고. Direct·Work·
Verify·Resume·project-state·managed-card landing·journal 문법·commit message는 바뀌지 않는다.

재검토 조건: 완료된 Adopt 뒤에도 현재 도메인 질문을 이해하거나 유지하려 흡수 입력이 필요하거나,
근거 부재가 live/card 좌표 검사를 약화하거나, committed `none` 경계가 state model 변경 없이 stale
baseline으로 route되지 않는 장면이 관측될 때.

## 기각된 안 — 지식층과 능력 문서

- **[DR-01 · v0.7.0]** **중간 인계 문서** — 반쪽 진실을 넘긴다.
- **[DR-05 · v0.7.0]** **열린 결정의 journal 이관** — 한 개념 두 거처. HANDOFF 이월 규칙으로 대체.
- **[DR-14 · v0.9.18]** **관측 캐시 등재 필드(arch 산출 형식의 observation_cache 줄)** — 독립 검증 3렌즈 수렴
  기각: 어떤 스킬의 어떤 규칙도 읽지 않는 소비자 없는 필드라 문자적 실행을 바꾸지 못하고,
  유일한 의미(사실의 캐시일 뿐, 결정은 devflow 소유)는 밖 기록 지위 선언의 중복이며,
  관측 사실의 거처를 이원화하는 표류 경로만 연다. 외부 관측 캐시의 형태는 프로젝트
  소유로 남긴다 — 두 실사용 프로젝트가 서로 다른 형태를 발명한 것이 형태를 정하지
  말라는 실증이다.
- **[DR-15 · v0.9.18]** **'재기준선' 명사의 정본 승격** — 0.9.10이 resume 소화 문맥과의 충돌로 이 용어를
  의도적으로 제거한 기록이 있고 그 사유가 여전히 성립한다. 명사 대신 서술형
  ("product 재실행")을 표에 실었다.
- **[DR-16 · v0.9.18]** **HANDOFF 형식 변경('다음 한 걸음'에 스킬 이름 허용)** — 기존 '열린 결정' 절과 반증
  행의 journal 대기 줄이 같은 경로를 이미 닫는다. 형식 불변.
- **[DR-17 · v0.9.18]** **옵시디언식 자유 링크 구조** — 무유계 읽기 기각 계보에 걸리고, 트리와 경쟁하는 제2
  구조(한 개념 두 거처)다. devflow의 관계 모델은 타입드 엣지(의존·좌표·읽을 것·해소
  카드)와 내용 운반 규율로 유지한다.
- **[DR-18 · v0.11.0]** **product.md에서 능력마다 전체 설계를 확장** — work이 매 카드마다 통독하는 Layer 0를 능력
  수에 비례해 키운다.
- **[DR-19 · v0.11.0]** **설계·검증 구역을 파일 둘로 분리** — 능력 하나에 경로 둘을 만들고 읽기와 개명 비용을
  두 배로 만든다.
- **[DR-20 · v0.11.0]** **첫 폐쇄를 앞당기거나 split이 문서를 생성** — 설계 기간과 split이 없는 브라운필드에서
  문서가 여전히 없고, 태스크 분해가 도메인 설계를 소유하게 된다.
- **[DR-21 · v0.11.0]** **arch가 만들고 split이 갱신** — split이 아는 태스크 구조는 설계 구역의 입력이 아니며 두
  작성자가 같은 byte를 소유한다.
- **[DR-22 · v0.11.0]** **설계 구역에 계획된 주 흐름·진입점도 기록** — 첫 폐쇄 뒤 같은 개념이 검증 구역에 다시
  생긴다. 계획된 진입점은 product.md·arch.md에 이미 있다.
- **[DR-23 · v0.11.0]** **두 구역의 7필드를 파일 끝 단일 블록에 둔다** — verify가 전면 교체하는 suffix 안의
  `Capability number`·`Design head`를 arch가 다시 고쳐야 해 고정 경계가 byte 소유를 나누지
  못한다. 설계 2필드와 검증 5필드의 물리 분리가 이 실패를 닫는다.
- **[DR-24 · v0.11.0]** **resume이 파일명만 보고 도메인 질문에 답한다고 간주** — 경로를 보여 주는 것은 AI가 그
  문서를 읽는 실행 분기가 아니다. 번호·이름으로 정확히 하나를 여는 별도 유계 분기가 필요하다.
- **[DR-25 · 기타]** **유지보수 재개봉 시 "관련 기록을 훑는다" 류의 규칙** — '관련'이 판단어라 문자적 AI가
  살찐 폴더 전체를 통독하는 읽기 폭발 위험. **무유계 읽기 규칙은 만들지 않는다.**
  (유계인 확정 문안은 아래 관찰 항목 참조.)
- **[DR-30 · v0.12.0]** **능력별 작업 노트 새 계층** — 중계 노트는 이미 네 층으로 존재한다(코드 · 미수확 카드 ·
  능력 문서 · Layer 0). 세 번째 집을 짓는 것이고, v0.11.0이 기각한 중계 노트 2차 핸드오프 층과
  경계가 맞닿는다.
- **[DR-31 · v0.12.0]** **미수확 카드의 진행 로그 전문 읽기 / 최근 N장만 읽기** — 전자는 실현 불가(카드마다 수백 줄),
  후자는 N이 판단어이자 임의 숫자다. `Covered cards` 여집합은 기계적 경계이고 이미 계산된다.
- **[DR-34 · v0.12.0]** **resume 보고에 능력 문서 신선도 한 줄 추가** — 기획에는 있었으나 채택하지 않았다. 그 줄이
  막는 잘못된 행동이 없다. 승인은 단계를 고를 뿐이고 지식을 소비하는 것은 work이며, work이
  같은 줄을 보고하고 가설은 사용 전 재확인이 강제된다. 대신 "보통 재개는 본문을 읽지 않는다"를
  고쳐야 했다. 사유 절과 대안 목록은 사용자가 실제로 다른 단위를 고르게 하므로 채택했다.
- **[DR-40 · v0.13.0]** **은퇴 시 `능력 관측` 처분을 위한 6필드 상태 기계** — 정본 30~50줄인데, 은퇴 커밋 앞의 게이트
  하나면 닫힌다. 은퇴에서 증거 기록을 지우는 옆 조항과 같은 모양이다.
- **[DR-43 · v0.13.0]** **능력 지식 기준선 판정 정본을 읽는 계약과 쓰는 계약으로 분할** — 시도했다가 자기 조건에
  따라 접었다. 조항×소비자 행렬이 깨끗하지 않다. 정체성과 기대 집합, 문서 계약, 메타데이터와
  신선도, 쓰기 주체와 교체 경계, v0.10 이관, 허용 한계가 전부 arch·adopt·verify·resume 모두가
  직접 읽는 것이고 그것이 파일의 대부분이다. 읽기 쪽이라던 resume은 경계 손상 복구와 begin
  커밋을 위해 쓰기 자격 규칙이 필요하고, 쓰기 쪽인 verify는 도메인 진입의 역할 입력이 필요하다.
  모든 소비자가 두 파일을 함께 읽게 되며 그것이 기록된 접는 조건이다. 폐기가 아니라 보류다.
- **[DR-45 · v0.13.0]** **골조 폐쇄가 관측을 검증 구역에 접어 넣는다** — 정해야 할 값이 여섯(언제·누가·어느 시점
  입력·어느 칸·미검증 내용의 표시·카드 목록)이고 진입점 자동 판별이 기계적이지 않아 구현 불가
  수준의 미정 상태였다. 골조의 검증 구역은 계속 `None.`이다 — 시나리오 검증을 받지 않으므로
  검증된 상태가 없는 것이 정직하다.
- **[DR-46 · v0.13.0]** **공용 부품의 소비 능력측 출처 보존 view** — 소비자 시점의 설명이지 공용 부품 자체의 설명이
  아니다. 공용 부품 지식은 이미 세 집(ADR · arch.md의 Risks · 골조 카드의 인계 줄)에 도달
  경로와 함께 산다.
- **[DR-47 · v0.17.0]** **Provisional 정의를 blocking 사실까지 넓혀 통일** — Provisional은 안전한
  기본값이 있고 나중 결과가 최적화만 바꾸는 follow-up 사실이다. `arch:58`의 "never push a
  blocking fact into Provisional"과 `planning-evidence:16-19`의 blocking/follow-up 분리가
  후보 가능성이나 검증을 좌우하는 사실을 결정 전에 멈춘다. 재제안은 이 경계를 먼저 반증해야 한다.
- **[DR-48 · v0.17.0]** **능력명과 폴더명의 대응 완화** — 철학 4번과 7원칙 1번의 "한 개념 한 단어",
  `verify:190-203`의 정확 경로 매핑 요구를 함께 깨며, 매핑 정본은 이미 arch.md의 `코드 구조`다.
  재제안은 네 근거를 모두 반증하고 verify가 매회 의미를 재해석하지 않는 기계 대응을 제시해야 한다.
- **[DR-49 · v0.17.0]** **Layer 0 초안에 `.wip.` 파일명 접미사 부여** — `principles:367-368`은
  확인 전 core-document 경로 변경을 금지하고, `:522-523`은 맨 `.wip.`를 무결성 이상으로 보며,
  `:509-511`은 product·arch·design·code-style·glossary의 진행 기록을 금지한다. 통합 봉쇄에서는
  Layer 0 변경 자체가 대기하므로 주 실패 장면에서도 작동하지 않는다. 재제안은 이 네 충돌과 복구
  비용을 먼저 반증해야 한다.
- **[DR-50 · v0.17.0]** **design을 외부 자료의 권위 통로로 사용** — `design:12`는 `frontend: none`이면
  design을 끝내고, `Design source`의 권위는 design scope에 한정된다. 서버·CLI·라이브러리의
  product·arch 사실에는 실행할 수 없는 하위 통로다. 재제안은 이 진입 게이트와 소유 범위를 먼저
  반증해야 한다.

## 정본 토큰

이 결정이 정의하거나 참조하는 정본 문자열이다. **번역하지 않는다** — 문장은 한국어라도
이 토큰은 grep 좌표이므로 원문 그대로 유지한다.

- `Code structure`
- `capability note`
