# @generated by Skill Rails
interface:
  display_name: "Split"
  short_description: "Task splitting. Opens the task tree (devflow/tree/) one layer at a time, creates task cards, and gets the execution proposal (order, parallelism, model tiers) approved by the user. Use for breaking down work, task decomposition, or planning what to do next."
  default_prompt: "Use $split for this task and follow its evidence boundary."
policy:
  allow_implicit_invocation: true
