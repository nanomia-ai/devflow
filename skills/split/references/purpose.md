# Purpose

Preserve the behavior of immutable repository sources `skills/split/SKILL.md` and `skills/split/SKILL_ko.md`, identified by their recorded source hashes, without silently changing their obligations.

Preserve split as the sole task-tree planner: consume principles project-state's exact current origin and active scopes, materialize one ordinary task or numbered research card per independently executable unit, and land one user-approved planning boundary without writing current K knowledge.

The reserved `00-project` root may contain many ordinary research-card branches. Each carries its exact Origin, begins pending, crosses the same proposal write-and-commit boundary, and routes to work only when canonical project-state reports effective approval for that exact card and origin.

Cards may list every affected knowledge owner, but split never creates knowledge-landing markers. Later confirmed work or research synthesis owns those markers only after a durable conclusion has a long-lived consumer.
