# Shared authoring policy index

The deploy-time policy owners are local. Start at [Local policy index](policy-index.md#local-policy-index), then open only the authoring, state, or delivery topic whose condition matches the current Decision. The index and topic links replace every legacy path-based ownership claim.
