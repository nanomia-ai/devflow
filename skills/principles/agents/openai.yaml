# @generated by Skill Rails
interface:
  display_name: "Principles"
  short_description: "Canonical devflow mechanics and evidence boundaries for explicit routing or recovery questions, ownership, knowledge landing, commits, and verification; explicit stage invocations are their own entry and do not require Principles preflight."
  default_prompt: "Use $principles for this task and follow its evidence boundary."
policy:
  allow_implicit_invocation: true
