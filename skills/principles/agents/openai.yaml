# @generated by Skill Rails
interface:
  display_name: "Principles"
  short_description: "Canonical devflow mechanics and evidence boundaries used by every devflow skill; use before any devflow stage or when routing, recovery, ownership, knowledge landing, commits, or verification are in question."
  default_prompt: "Use $principles for this task and follow its evidence boundary."
policy:
  allow_implicit_invocation: true
