# @generated by Skill Rails
interface:
  display_name: "Design"
  short_description: "Design planning (optional stage). Decides the UI approach, source, token and component strategies, decomposition axis, and review surface, and creates devflow/project/design.md. Use for design systems, UI style, or component-library decisions."
  default_prompt: "Use $design for this task and follow its evidence boundary."
policy:
  allow_implicit_invocation: true
