# @generated by Skill Rails
interface:
  display_name: "Work"
  short_description: "Implementation. Takes one task card, codes it, keeps the progress log on disk, runs the completion signal, and commits. Use when explicitly invoked, when another devflow skill routes here, or for work in an existing devflow-managed project."
  default_prompt: "Use $work for this task and follow its evidence boundary."
policy:
  allow_implicit_invocation: true
