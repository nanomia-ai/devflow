# @generated by Skill Rails
interface:
  display_name: "Adopt"
  short_description: "Existing-project adoption. In a repository that already has code, traces one representative flow per capability candidate and reverse-derives Layer 0 and capability design zones. Use only with explicit devflow intent—direct invocation, naming devflow, or asking to adopt an existing codebase into devflow, reverse-derive Layer 0, or create or repair devflow capability documents."
  default_prompt: "Use $adopt for this task and follow its evidence boundary."
policy:
  allow_implicit_invocation: true
