export const SPEC = { version: "5", id: "product", profile: "single", imports: [] };
export const OBSERVATIONS = {
  "entry.state": { collector: "state.product-entry", domain: ["new", "existing", "brownfield-no-product", "uncommitted-layer0", "unknown"] },
  "product.file": { collector: "state.product-file", domain: ["absent", "current", "unknown"] },
  "glossary.state": { collector: "state.glossary", domain: ["missing", "current", "unknown"] },
  "product.request": { collector: "state.product-request", domain: ["none", "product-re-run", "unknown"] },
  "research.card.state": { collector: "state.project-research-state", domain: ["none", "pending", "effective", "unknown"] },
  "compatible.owner": { collector: "state.compatible-owner", domain: ["none", "product", "glossary", "invalid"] },
  "compatible.landing": { collector: "state.compatible-landing", domain: ["none", "pending", "satisfied", "invalid"] },
  "research.need": { judged: true, domain: ["inline", "needed", "blocked", "settled"] },
  "glossary.action": { decided: true, domain: ["ask", "write"] },
  "approval.action": { decided: true, domain: ["ask", "commit"] }
};
export const FORMATS = {};
export const TEMPLATES = {
  productProposal: { file: "templates/product-proposal.md", fields: { serviceName: "line", identity: "block", problem: "block", approach: "block", capabilityRows: "generated", boundary: "block", successCriteria: "list", accessPoints: "list", glossaryDelta: "list", knowledgeDelta: "block" }, sections: [] },
  productConfirmed: { file: "templates/product-confirmed.md", fields: { serviceName: "line", identity: "block", problem: "block", approach: "block", capabilityRows: "generated", boundary: "block", successCriteria: "list", accessPoints: "list", openQuestions: "block", interface: "line" }, sections: [] },
  glossary: { file: "templates/glossary.md", fields: { terms: "generated" }, sections: [] },
  knowledgeNode: { file: "templates/knowledge-node.md", fields: { title: "line", openWhen: "line", about: "line", body: "block", sourceBasis: "generated" }, sections: [] }
};
export const ORDERS = { planning: ["compatible-feedback", "orient", "research", "glossary", "propose-and-confirm"] };
export const OWNERSHIP = { ".devflow/project/product.md": "product", ".devflow/project/product": "product", ".devflow/project/product/**": "product", ".devflow/project/glossary.md": "product", ".devflow/journal.md#compatible-feedback-pending": "product-on-exact-owner-landing", ".devflow/journal.md#product-re-run-pending": "product-on-addressed-binding", ".devflow/tree/00-project": "external.direct", ".devflow/project/capabilities/**": "external.arch" };
export const GUARDS = [
  { id: "raw-state-unavailable", reads: ["entry.state", "product.file", "glossary.state", "product.request", "research.card.state"], acceptsUnknown: [], when: s => s.entry.state === "unknown" || s.product.file === "unknown" || s.glossary.state === "unknown" || s.product.request === "unknown" || s.research.card.state === "unknown", then: "BLOCK", body: "guard: raw-state-unavailable" },
  { id: "existing-project-research", reads: ["research.card.state"], acceptsUnknown: [], when: s => s.research.card.state === "pending" || s.research.card.state === "effective", then: "ROUTE:direct", body: "guard: existing-project-research" },
  { id: "compatible-feedback-shape", reads: ["compatible.owner", "compatible.landing"], acceptsUnknown: [], when: s => s.compatible.owner === "invalid" || s.compatible.landing === "invalid", then: "BLOCK", body: "guard: compatible-feedback-shape" }
];
export const STAGES = [
  { id: "compatible-feedback", reads: ["compatible.owner"], acceptsUnknown: [], done: s => s.compatible.owner === "none", table: "compatibleFeedback", reentry: "rejudge", branches: {
    "write-product": [["RUN", { action: "project existing product/K headers under the exact product owner" }], ["WRITE", { artifact: "product", source: "compatible-feedback exact coordinates", preserves: ["source", "background", "why", "conclusion", "implication"] }], ["WRITE", { artifact: "knowledgeNodes", template: "knowledgeNode", selection: "changed units already owned by existing product/K only", zeroAllowed: true }], ["RUN", { action: "validate exact changed product/K paths" }], "WAIT"],
    "write-glossary": [["WRITE", { artifact: "glossary", source: "compatible-feedback exact coordinates", preserves: ["source", "background", "why", "conclusion", "implication"] }], "WAIT"],
    land: [["RUN", { action: "delete-byte-identical-compatible-feedback-marker" }], ["COMMIT", { scope: "compatible-feedback-owner-and-marker", touches: ["compatible.owner", "exact changed product/K paths", ".devflow/journal.md"] }], "ROUTE:resume"]
  }, body: "stage: compatible-feedback" },
  { id: "orient", reads: ["entry.state"], acceptsUnknown: [], done: s => !["brownfield-no-product", "uncommitted-layer0"].includes(s.entry.state), effects: ["ROUTE:resume"], reentry: "rejudge", body: "stage: orient" },
  { id: "research", reads: ["research.need"], acceptsUnknown: [], done: s => s.research.need === "inline" || s.research.need === "settled", needs: ["research.need"], reentry: "rejudge", branches: { needed: [["RUN", { action: "record-durable-pre-product-research-origin" }], "ROUTE:direct"], blocked: ["WAIT"] }, body: "stage: research" },
  { id: "glossary", reads: ["product.file", "glossary.state", "product.request"], acceptsUnknown: [], done: s => s.product.file === "absent" || s.glossary.state === "current" || s.product.request === "product-re-run", needs: ["glossary.action"], reentry: "rejudge", branches: { ask: ["ASK"], write: [["WRITE", { artifact: "glossary", template: "glossary" }], ["COMMIT", { artifact: "glossary", boundary: "layer-0-completion" }], "ROUTE:resume"] }, body: "stage: glossary" },
  { id: "propose-and-confirm", reads: ["product.file", "glossary.state", "product.request"], acceptsUnknown: [], done: s => s.product.file === "current" && s.glossary.state === "current" && s.product.request === "none", needs: ["approval.action"], table: "confirmationRoute", reentry: "rejudge", branches: {
    ask: [["RUN", { action: "project existing product/K headers under the exact product owner" }], ["REPORT", { template: "productProposal" }], "ASK"],
    "commit-initial": [["RUN", { action: "project existing product/K headers under the exact product owner" }], ["WRITE", { artifact: "product", template: "productConfirmed" }], ["WRITE", { artifact: "glossary", template: "glossary" }], ["WRITE", { artifact: "knowledgeNodes", template: "knowledgeNode", selection: "approved sourced product-owned units; existing loci stay in place and new K requires the shared capsule boundary", zeroAllowed: true }], ["RUN", { action: "validate exact changed product/K paths" }], ["COMMIT", { artifact: "product", boundary: "confirmed-layer-0", touches: [".devflow/project/product.md", ".devflow/project/glossary.md", "exact changed product/K paths"] }], "ROUTE:arch"],
    "commit-rerun": [["RUN", { action: "project existing product/K headers under the exact product owner" }], ["WRITE", { artifact: "product", template: "productConfirmed" }], ["WRITE", { artifact: "glossary", template: "glossary" }], ["WRITE", { artifact: "knowledgeNodes", template: "knowledgeNode", selection: "approved sourced product-owned units; existing loci stay in place and new K requires the shared capsule boundary", zeroAllowed: true }], ["RUN", { action: "validate exact changed product/K paths and apply every matching canonical discovery-update action and delete only the addressed product re-run pending lines; preserve unaddressed lines" }], ["COMMIT", { authority: "external.principles", boundary: "product-rerun-binding", touches: [".devflow/project/product.md", ".devflow/project/glossary.md", ".devflow/journal.md", "exact changed product/K paths", "matching canonical discovery-update targets"] }], "ROUTE:resume"],
    "BLOCK:confirmation-state": ["BLOCK"]
  }, body: "stage: propose-and-confirm" }
];
export const TABLES = {
  compatibleFeedback: { exclusive: true, rows: [
    { state: "write-product", reads: ["compatible.owner", "compatible.landing"], acceptsUnknown: [], when: s => s.compatible.owner === "product" && s.compatible.landing === "pending" },
    { state: "write-glossary", reads: ["compatible.owner", "compatible.landing"], acceptsUnknown: [], when: s => s.compatible.owner === "glossary" && s.compatible.landing === "pending" },
    { state: "land", reads: [], acceptsUnknown: [], when: () => true }
  ] },
  confirmationRoute: { exclusive: true, rows: [
    { state: "ask", reads: ["approval.action"], acceptsUnknown: [], when: s => s.approval.action === "ask" },
    { state: "commit-initial", reads: ["approval.action", "product.file", "product.request"], acceptsUnknown: [], when: s => s.approval.action === "commit" && s.product.file === "absent" && s.product.request === "none" },
    { state: "commit-rerun", reads: ["approval.action", "product.file", "product.request"], acceptsUnknown: [], when: s => s.approval.action === "commit" && s.product.file === "current" && s.product.request === "product-re-run" },
    { state: "BLOCK:confirmation-state", reads: [], acceptsUnknown: [], when: () => true }
  ] }
};
export const ARTIFACTS = { product: { path: ".devflow/project/product.md", writer: "product", readers: ["stage.compatible-feedback", "stage.propose-and-confirm", "external.arch", "external.design"], template: "productConfirmed" }, knowledgeNodes: { path: ".devflow/project/product", writer: "product", readers: ["stage.compatible-feedback", "stage.propose-and-confirm", "external.direct", "external.resume"], template: "knowledgeNode" }, glossary: { path: ".devflow/project/glossary.md", writer: "product", readers: ["stage.compatible-feedback", "stage.glossary", "stage.propose-and-confirm", "external.arch"], template: "glossary" }, compatibleFeedbackMarker: { path: ".devflow/journal.md#compatible-feedback-pending", writer: "product", readers: ["stage.compatible-feedback"] }, journal: { path: ".devflow/journal.md", writer: "external.principles", readers: ["stage.compatible-feedback", "stage.research", "stage.propose-and-confirm", "guard.existing-project-research"] }, productRerunMarker: { path: ".devflow/journal.md#product-re-run-pending", writer: "product", readers: ["stage.propose-and-confirm"] }, projectResearch: { path: ".devflow/tree/00-project", writer: "external.direct", readers: ["stage.research", "guard.existing-project-research", "external.direct"] } };
export const ROLES = {};
export const READ_FIRST = [{ body: "why: purpose", path: "references/purpose.md" }, { body: "why: evidence-boundary", path: "references/legacy-source.md" }];
export const DECLARATIONS = { "capability-row-schema": { value: "Generated capabilityRows use C<number> <name> - User outcome: <outcome> - Needed for success: <reason>. The stable number is generated from append-only position; the row neither assigns responsibility nor duplicates the separate Success criteria acceptance signal.", consumer: "templates.productProposal,templates.productConfirmed" }, "coverage-review-disposition": { value: "Every imported source obligation has a concrete forward home below; source provenance is evidence for that home, not a runtime state that may remain unresolved.", consumer: "migration:source-coverage" }, "coverage-invocation-identity": { value: "Product is invoked only by direct Product invocation or explicit new-project/product-planning intent. It identifies the service problem, capability composition, boundary, and product artifact.", consumer: "migration:source-coverage" }, "coverage-glossary-canon": { value: "`glossary.md` is the durable project-language canon. Record each interview-confirmed important term as one exact `term: definition` line, prevent synonym forks, and do not duplicate definitions in capability documents or cards.", consumer: "migration:source-coverage" }, "coverage-required-inputs": { value: "Read the canon and planning-evidence discipline first. When present, read the journal, product, and glossary in full; read arch only to route a completed re-run.", consumer: "migration:source-coverage" }, "coverage-pending-re-run-evidence": { value: "Order product-re-run pending lines by timestamp then journal order, decode each source text, present it for reconfirmation, and report malformed or undecodable reserved lines as integrity anomalies. Never guess from, delete, or start a re-run on an unconfirmed pending line.", consumer: "migration:source-coverage" }, "coverage-service-boundary": { value: "Product is service planning, not development planning. Record a user-confirmed development choice and its reason as an attributed journal handoff for arch, say that arch confirms it, and return to product questions; never copy it into product.md.", consumer: "migration:source-coverage" }, "coverage-six-judgments": { value: "The complete product plan keeps Problem, Approach, Capability composition, Boundary, Success criteria, and Screens & access points together as current truth.", consumer: "migration:source-coverage" }, "coverage-judgment-problem": { value: "Problem answers who suffers, because of what, and how badly.", consumer: "migration:source-coverage" }, "coverage-judgment-approach": { value: "Approach records why the chosen approach wins, discarded approaches, and the priority when goals collide.", consumer: "migration:source-coverage" }, "coverage-judgment-capabilities": { value: "Capability composition identifies the chunks needed to solve the problem; their names remain the architecture modules and tree-folder names.", consumer: "migration:source-coverage" }, "coverage-judgment-boundary": { value: "Boundary states the MVP limit and explicitly names what will not be built.", consumer: "migration:source-coverage" }, "coverage-judgment-success": { value: "Success criteria state verifiable acceptance conditions; an unverifiable criterion must be asked again.", consumer: "migration:source-coverage" }, "coverage-judgment-access": { value: "Screens and access points state where users reach the service.", consumer: "migration:source-coverage" }, "coverage-evidence-frontier": { value: "Before questions, classify unknowns with the planning-evidence discipline. Settle repository, external-contract, and execution facts through its source and research branches; do not ask facts that change no option.", consumer: "migration:source-coverage" }, "coverage-blocking-fact": { value: "For a blocking conflicted or unavailable fact, report the conflict or absence and the decision it would change, then stop only that dependent frontier.", consumer: "migration:source-coverage" }, "coverage-research-scope": { value: "Research explicitly requested by the owner stays within that scope and then returns to the original frontier unless it blocks it. Automatic research runs only when an unsettled result changes a candidate, recommendation, Layer 0 field, or verifiability; stack and database choices remain arch decisions.", consumer: "migration:source-coverage" }, "coverage-frontier-batching": { value: "Batch only mutually independent questions, normally three to five. Before another frontier and before final confirmation, run the pre-commitment review; with no candidate, add neither output nor question.", consumer: "migration:source-coverage" }, "coverage-durable-reasons": { value: "Place each confirmed reason and discarded alternative in its owning product section. Attach measurement conditions and rerun command for numeric grounds, then individually reconfirm any newly confirmed reason absent from product.md before final confirmation.", consumer: "migration:source-coverage" }, "coverage-defaulted-interview": { value: "Every question states a recommendation and that an unanswered item proceeds with its default.", consumer: "migration:source-coverage" }, "coverage-scope-rounds": { value: "Use one or two shallow rounds for one to three capabilities, three deeper rounds for four to seven, and stop to propose a split at eight or more capabilities.", consumer: "migration:source-coverage" }, "coverage-capability-exclusion": { value: "For every capability ask whether all success criteria can still pass without it; a yes marks an MVP-exclusion candidate.", consumer: "migration:source-coverage" }, "coverage-conversation-summary": { value: "When the round budget is exhausted, summarize confirmed content and remaining questions once in conversation only; that summary creates no file, state, or commit.", consumer: "migration:source-coverage" }, "coverage-meaning-density": { value: "Do not create a repeated format. Problem owns current state, Approach owns choice, reason, and discarded alternatives, Success criteria owns observable result, and each capability line carries its stable number, name, user outcome, and needed-for-success reason once; responsibility and an acceptance signal are not substitutes for those fields.", consumer: "migration:source-coverage" }, "coverage-product-schema": { value: "The confirmed product schema has a service name, fixed-form identity paragraph, the six judgment headings, Open questions, and one allowed interface value. It has no Approval field.", consumer: "migration:source-coverage" }, "coverage-glossary-output": { value: "Create the glossary beside a confirmed product using the exact `term: definition` line form for terms fixed during the interview.", consumer: "migration:source-coverage" }, "coverage-glossary-only-recovery": { value: "If a confirmed product exists and only its glossary is missing, do not repeat the interview or modify product.md. Copy terms whose meanings are fixed, ask one batch only for undefined terms, and create only glossary.md.", consumer: "migration:source-coverage" }, "coverage-capability-continuity": { value: "Capability rows are append-only and never reordered or deleted; additions go last and a missing number derives from position.", consumer: "migration:source-coverage" }, "coverage-retirement-record": { value: "On maintenance retirement keep the numbered row as a struck-through name with date and evidence pointer. Apply the canon's stale tree or waiting-file representation only where it exists; create the no-tree waiting file only for `Brownfield: no`, preserve the baseline, and let the next design refresh report stored consumers.", consumer: "migration:source-coverage" }, "coverage-retirement-observation": { value: "Before retirement approval, run the canonical retirement observation gate, including its enumeration, owner question, and same-commit landing.", consumer: "migration:source-coverage" }, "coverage-approval-gate": { value: "A proposed plan is reported and explicitly approved before any canonical product write. Initial confirmation lands on the Layer 0 boundary; a re-run uses its binding-decision boundary.", consumer: "migration:source-coverage" }, "coverage-re-run-binding": { value: "For a confirmed pending re-run, apply every matching canonical discovery-update action, all required renames and markers, the product change, and deletion only of addressed pending lines in one binding-decision commit. On interruption, delete none and preserve unaddressed lines.", consumer: "migration:source-coverage" }, "coverage-next-stage-routing": { value: "After first creation route to arch. After a re-run, contradictions with arch route to arch; otherwise capability-list, name, or boundary changes route to the appropriate Arch design refresh without repeating Layer 0, and unchanged plans route to resume when a tree exists or direct when it does not.", consumer: "migration:source-coverage" }, profile: { value: "p2", consumer: "build:profile" }, projectResearch: { value: "00-project research-card planning and approval are direct-owned; work executes approved research, and product synthesizes settled evidence into product judgment. Product writes neither that tree nor implementation cards.", consumer: "stage:research" }, currentTruth: { value: "A report is provisional. product.md becomes current only when explicit approval writes the confirmed schema and the matching Layer 0 or product-re-run binding commit lands.", consumer: "stage:propose-and-confirm" }, productSchema: { value: "product.md uses the legacy six-judgment schema and contains no Approval field.", consumer: "stage:propose-and-confirm" }, rerunBoundary: { value: "A revision is selected only by the canonical product-re-run request state; its confirmed write uses the principles binding boundary, including the addressed pending lines and required discovery updates.", consumer: "stage:propose-and-confirm" }, capabilityIdentity: { value: "Capability rows append; retirement retains its number and row. Product writes neither tree numbers nor capability retirement artifacts.", consumer: "external.direct" }, developmentHandoff: { value: "A confirmed development choice and its reason remain an attributed journal handoff for arch; they do not enter product.md.", consumer: "external.arch" }, knowledgeWriter: { value: "Product writes only product/K selected by the shared owner batch; existing units stay at their current loci, new nodes require sourced independent depth, and capability K remains Arch-owned.", consumer: "stage.compatible-feedback|stage.propose-and-confirm" } };
export const DEFERRED = [];
