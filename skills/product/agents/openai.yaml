# @generated by Skill Rails
interface:
  display_name: "Product"
  short_description: "Service-planning interview. Defines the problem, capability composition, and boundaries of a new project, producing devflow/project/product.md. Use when starting a new project, planning a service, defining a product, or deciding MVP scope."
  default_prompt: "Use $product for this task and follow its evidence boundary."
policy:
  allow_implicit_invocation: true
