# @generated by Skill Rails
interface:
  display_name: "Product"
  short_description: "Service-planning interview. Defines the problem, capability composition, and boundaries of a new project, producing devflow/project/product.md. Use only with direct Product invocation or explicit intent to plan a new project's product or service, define its problem, capability composition, and boundary, or create devflow/project/product.md. A generic devflow mention is not a Product trigger; Principles owns generic entry."
  default_prompt: "Use $product for this task and follow its evidence boundary."
policy:
  allow_implicit_invocation: true
