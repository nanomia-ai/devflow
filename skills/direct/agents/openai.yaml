# @generated by Skill Rails
interface:
  display_name: "Direct"
  short_description: "Work direction. Determines planning depth and executable unit size, materializes research or task cards, and gets the execution proposal (order, parallelism, model tiers, Work handoff) approved by the user. Use when explicitly invoked, when another devflow skill routes here, or for work in an existing devflow-managed project."
  default_prompt: "Use $direct for this task and follow its evidence boundary."
policy:
  allow_implicit_invocation: true
