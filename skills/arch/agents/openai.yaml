# @generated by Skill Rails
interface:
  display_name: "Arch"
  short_description: "Development planning. Takes product.md and decides components, stack, code structure, and the verify channel, then writes capability design zones. Use for stack selection, architecture design, or creating and repairing capability documents in a new project."
  default_prompt: "Use $arch for this task and follow its evidence boundary."
policy:
  allow_implicit_invocation: true
