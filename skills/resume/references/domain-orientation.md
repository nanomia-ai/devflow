# Domain orientation

Domain entry is read-only and precedes normal routing only for a domain request. It resolves the selected capability through the snapshot, opens only shape-valid bounded documents and declared ADR or capsule inputs, reports uncertainty rather than inventing an owner, and returns to normal routing for implementation.
