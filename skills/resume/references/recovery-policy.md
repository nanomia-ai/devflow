# Recovery policy

Read the canonical resume recovery branch for the selected marker or boundary. For a design marker with `recovery=producer`, read the named producer source before selecting `route-work`; with `recovery=external`, report the recorded external recovery and stop without rewriting. Keep every recorded marker field intact; absent or invalid recovery stays with the bounded arch-or-adopt policy, and a folder-boundary or digest re-entry follows only a real required boundary commit.
