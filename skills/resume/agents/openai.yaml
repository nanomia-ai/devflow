# @generated by Skill Rails
interface:
  display_name: "Resume"
  short_description: "Resume and domain entry. Restores a new session from disk and continues to the next stage, or finds a capability document by number or name and explains the domain. Use for session restarts, continuing work, status checks, or capability and domain onboarding questions."
  default_prompt: "Use $resume for this task and follow its evidence boundary."
policy:
  allow_implicit_invocation: true
