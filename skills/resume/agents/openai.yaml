# @generated by Skill Rails
interface:
  display_name: "Resume"
  short_description: "Resume and domain entry. Restores a new session from disk and continues to the next stage, routes canonical knowledge markers to Arch while preserving their declared writers as provenance and residual markers for replay, or finds a capability document by number or name and explains the domain. Use when explicitly invoked, when another devflow skill routes here, or for work in an existing devflow-managed project."
  default_prompt: "Use $resume for this task and follow its evidence boundary."
policy:
  allow_implicit_invocation: true
