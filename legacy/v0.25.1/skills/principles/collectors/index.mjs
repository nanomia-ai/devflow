// Principles intentionally collects no project state. Request partition and tweak-gate
// answers are bounded conversation judgments; calculateState is consumed by resume/stages.
export const collectors = Object.freeze({});

export const snapshotBasis = null;
