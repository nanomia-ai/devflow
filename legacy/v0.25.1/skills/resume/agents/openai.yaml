# @generated by Skill Rails
interface:
  display_name: "Resume"
  short_description: "Resume and domain entry. Reads canonical disk state to locate a devflow-managed project's current position, recover an interrupted transition, and either report it or route to the stage that owns the next step. On request, opens one capability document by number or name and explains that domain with bounded reads. Another named devflow stage enters directly. Use when explicitly invoked, when another devflow skill routes here, or for work in an existing devflow-managed project."
  default_prompt: "Use $resume for this task and follow its evidence boundary."
policy:
  allow_implicit_invocation: true
