# authoring card — resume
Purpose: restore a cold session from the structured state kernel, distinguish continuation from domain orientation, and route exactly once.
Failure scene: a new AI scans the repository, reconstructs journal rules, confuses handoff with onboarding, or loops through principles instead of continuing the canonical route.
Observations: intent.scope, state.canonicalNext, and state.compatibleWriter, with schema-2 state, zones, and facts collected from an explicit project root. Every knowledge marker routes to Arch; its declared writer remains provenance rather than a stage selector.
Terminals: BLOCK unrecognized state; ASK for a required owner choice; ROUTE to the canonical stage; WAIT after a recovery boundary; DONE for bounded orientation/status answers.
Guards: unrecognized-state.
Stages: recovery-action; scope-entry.
Effects: read only named current artifacts, repair the exact handoff/digest boundary when selected, report orientation, or route once.
Passengers: current route, active task or marker, handoff freshness, domain identity, blockers, and user scope.
Artifacts: glossary, product, selected capability, selected cards, HANDOFF, and digest, with readers declared in the spec.
Templates: result and orientation.
Ownership: calculateState owns canonical facts; resume owns entry/orientation and recovery routing; stage skills own their writes.
Consumer consumption sets: continuation reads schema-2 route plus exact active artifacts and HANDOFF; domain orientation reads product/glossary and one named capability subtree; status reads the bounded state report; recovery reads only its selected policy and boundary files.
Consumer closure check: every static project artifact is declared in ARTIFACTS with readers; dynamic paths come only from schema-2 facts; no CLI or raw-journal fallback is needed.
Deferred: none.
