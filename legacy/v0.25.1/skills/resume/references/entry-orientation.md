# Entry orientation

Run the canonical state snapshot before normal routing and read the shared glossary when it exists. The snapshot's priority order is the durable disk fact; a named card, session deferral, network refresh, unavailable tool, or missing source path is handled only within the source boundary that names it.
