# @generated by Skill Rails
interface:
  display_name: "Verify"
  short_description: "Verification. Checks acceptance criteria at the capability and product level through real execution. Use when explicitly invoked, when another devflow skill routes here, or for work in an existing devflow-managed project."
  default_prompt: "Use $verify for this task and follow its evidence boundary."
policy:
  allow_implicit_invocation: true
