# Legacy atom ledger

Each ledger row preserves its source span and hash, then points to the runtime rule, flexible judgment, fixture, or declared artifact that carries the same obligation. The ledger is the bidirectional migration index; it does not create a second design hierarchy.
